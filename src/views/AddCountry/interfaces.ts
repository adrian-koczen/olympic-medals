export type MedalsCount = {
  total: number;
  bronze: number;
  silver: number;
  gold: number;
};

export enum Medals {
  bronze = "medals.bronze",
  silver = "medals.silver",
  gold = "medals.gold",
}
